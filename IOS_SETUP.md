# راهنمای اجرا روی iPhone 📱

## روش 1: با Expo Go (پیشنهادی) ✅

### مرحله 1: نصب Expo Go
1. **App Store** را روی iPhone باز کنید
2. **جستجو**: "Expo Go"
3. **نصب** اپلیکیشن Expo Go (ساخته شده توسط Expo)
4. **باز کردن** Expo Go

### مرحله 2: اسکن QR Code
1. در ترمینال Cursor، **QR Code** نمایش داده می‌شود
2. در iPhone:
   - **دوربین** را باز کنید (Camera app)
   - QR Code را اسکن کنید
   - یا در Expo Go: **"Scan QR code"** را بزنید
3. برنامه شروع به دانلود و اجرا می‌شود

### مرحله 3: اطمینان از اتصال
⚠️ **مهم**: iPhone و کامپیوتر باید در **همان شبکه WiFi** باشند!

---

## روش 2: iOS Simulator (اگر Mac دارید)

### مرحله 1: نصب Xcode
1. از **App Store** نصب کنید
2. یا از **developer.apple.com** دانلود کنید

### مرحله 2: اجرا در Simulator
```bash
# در ترمینال:
npx expo start --ios
```

این به صورت خودکار iOS Simulator را باز می‌کند.

---

## روش 3: Tunnel Mode (اگر WiFi مشترک ندارید)

```bash
# در ترمینال Ctrl+C بزنید
npx expo start --tunnel
```

این از اینترنت استفاده می‌کند (کندتر اما کار می‌کند).

---

## 🔧 اگر مشکل داشتید

### مشکل: "Unable to connect to server"

**راه‌حل 1: Tunnel Mode**
```bash
npx expo start --tunnel
```

**راه‌حل 2: بررسی Firewall**
- System Preferences → Security & Privacy → Firewall
- مطمئن شوید Expo اجازه دسترسی دارد

### مشکل: "Location permission denied"

در تنظیمات iPhone:
- Settings → Privacy → Location Services
- Expo Go → Allow While Using App

### مشکل: "Notifications not working"

در تنظیمات iPhone:
- Settings → Notifications → Expo Go
- Allow Notifications را فعال کنید

---

## ✅ ویژگی‌های iOS

- ✅ همه صفحات کار می‌کنند
- ✅ Location و Compass
- ✅ Notifications
- ✅ Haptic Feedback
- ✅ همه ویژگی‌ها

---

## 📝 نکات

- اولین بار ممکن است 1-2 دقیقه طول بکشد
- اگر کد را تغییر دهید، Expo Go خودکار reload می‌کند
- برای توقف: در ترمینال `Ctrl + C` بزنید
