# 🚀 راهنمای سریع: راه‌اندازی Supabase

## ✅ مرحله 1: تنظیمات انجام شد

فایل `.env` با اطلاعات Supabase شما ایجاد شده است:
- ✅ URL: `https://igsmyoghkkyetsyqbqlm.supabase.co`
- ✅ Anon Key: تنظیم شده

## 📋 مرحله 2: اجرای Migration (ضروری!)

برای ایجاد جداول دیتابیس:

1. به [Supabase Dashboard](https://supabase.com/dashboard) بروید
2. پروژه خود را انتخاب کنید
3. در منوی سمت چپ، **SQL Editor** را انتخاب کنید
4. فایل `supabase/migrations/001_initial_schema.sql` را باز کنید
5. **تمام محتوای فایل** را کپی کنید
6. در SQL Editor، محتوا را Paste کنید
7. روی دکمه **Run** (یا F5) کلیک کنید
8. منتظر بمانید تا Migration اجرا شود (چند ثانیه)

**نکته مهم**: اگر خطایی دیدید، ممکن است بعضی جداول از قبل وجود داشته باشند. این مشکلی نیست.

## 📚 مرحله 3: Import مقالات

بعد از اجرای Migration:

```bash
node scripts/seedArticlesWeb.js
```

این اسکریپت:
- ✅ 7 نویسنده را اضافه می‌کند
- ✅ 14 مقاله (7 دری + 7 پشتو) را اضافه می‌کند

## 🔄 مرحله 4: Restart Expo

```bash
# در ترمینال Metro، Ctrl+C بزنید (اگر در حال اجرا است)
npx expo start --clear
```

## ✅ مرحله 5: تست در اپلیکیشن

1. اپلیکیشن را Refresh کنید
2. به تب **"مقالات"** بروید
3. مقالات باید نمایش داده شوند! 🎉

## 🔍 بررسی وضعیت

برای بررسی اینکه همه چیز درست است:

```bash
node scripts/check-supabase-status.js
```

## 🐛 عیب‌یابی

### مقالات نمایش داده نمی‌شوند:
1. ✅ مطمئن شوید Migration اجرا شده است
2. ✅ مطمئن شوید `node scripts/seedArticlesWeb.js` بدون خطا اجرا شده
3. ✅ در Supabase Dashboard، Table `articles` را بررسی کنید
4. ✅ Expo را restart کنید: `npx expo start --clear`

### خطای "Permission denied":
- در Supabase Dashboard > Authentication > Policies
- مطمئن شوید که RLS policies برای خواندن مقالات تنظیم شده‌اند

---

**بعد از این مراحل، مقالات باید نمایش داده شوند!** 🎯
