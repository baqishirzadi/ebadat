# راهنمای اجرا روی Android 📱

## مرحله 1: نصب Expo Go

1. **باز کردن Play Store** روی گوشی Android
2. **جستجو**: "Expo Go"
3. **نصب** اپلیکیشن Expo Go (ساخته شده توسط Expo)
4. **باز کردن** Expo Go

## مرحله 2: اتصال به سرور

### روش 1: اسکن QR Code (پیشنهادی) ✅

1. در ترمینال Cursor، یک **QR Code** نمایش داده می‌شود
2. در اپ **Expo Go** روی گوشی:
   - دکمه **"Scan QR code"** را بزنید
   - یا از منو: **"Scan QR code"**
3. **QR Code** را اسکن کنید
4. برنامه شروع به دانلود و اجرا می‌شود

### روش 2: اتصال دستی

1. در Expo Go روی گوشی:
   - دکمه **"Enter URL manually"** را بزنید
2. در ترمینال، آدرس `exp://` را کپی کنید
   - مثلاً: `exp://192.168.1.100:8081`
3. آدرس را در Expo Go وارد کنید

## مرحله 3: اطمینان از اتصال

⚠️ **مهم**: گوشی و کامپیوتر باید در **همان شبکه WiFi** باشند!

اگر QR Code کار نکرد:
- مطمئن شوید هر دو به یک WiFi متصل هستند
- فایروال کامپیوتر را بررسی کنید
- از USB debugging استفاده کنید (پایین توضیح داده شده)

## 🔧 اگر مشکل داشتید

### مشکل: "Unable to connect to server"

**راه‌حل 1: Tunnel Mode**
```bash
# در ترمینال Ctrl+C بزنید
npx expo start --tunnel
```
این روش کندتر است اما از اینترنت استفاده می‌کند.

**راه‌حل 2: USB Debugging**
```bash
# USB را به کامپیوتر وصل کنید
# USB Debugging را در تنظیمات Developer Options فعال کنید
npx expo start --android
```

### مشکل: "Metro bundler error"

```bash
# Cache را پاک کنید
npx expo start --clear
```

### مشکل: "Location permission denied"

در تنظیمات Android:
- Settings → Apps → Expo Go → Permissions
- Location → Allow

## ✅ بعد از اجرا

برنامه باید روی گوشی شما باز شود و می‌توانید:
- ✅ همه صفحات را ببینید
- ✅ Location و Compass را تست کنید
- ✅ Notifications را تست کنید
- ✅ همه ویژگی‌ها را امتحان کنید

## 📝 نکات

- اولین بار ممکن است 1-2 دقیقه طول بکشد
- اگر کد را تغییر دهید، Expo Go خودکار reload می‌کند
- برای توقف: در ترمینال `Ctrl + C` بزنید
