# 🚀 راهنمای سریع: فعال‌سازی مقالات

## مشکل
مقالات در اپلیکیشن نمایش داده نمی‌شوند.

## علت
Firebase تنظیم نشده است (فایل `.env` وجود ندارد).

## راه‌حل (3 مرحله)

### 1️⃣ تنظیم Firebase
```bash
# ایجاد فایل .env
cp .env.example .env

# سپس فایل .env را باز کنید و اطلاعات Firebase را وارد کنید
# برای راهنمایی کامل: SETUP_FIREBASE_NOW.md را ببینید
```

### 2️⃣ Import مقالات
```bash
node scripts/seedArticlesWeb.js
```

### 3️⃣ Restart Expo
```bash
npx expo start --clear
```

## بررسی وضعیت
```bash
node scripts/check-firebase-status.js
```

---
**راهنمای کامل:** `SETUP_FIREBASE_NOW.md`
