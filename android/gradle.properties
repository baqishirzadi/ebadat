# Android Gradle project properties (tracked, safe defaults only).
# Do not put release signing secrets in this file.
# Use `android/keystore.properties` (gitignored) or environment variables:
#   EBADAT_RELEASE_STORE_FILE
#   EBADAT_RELEASE_STORE_PASSWORD
#   EBADAT_RELEASE_KEY_ALIAS
#   EBADAT_RELEASE_KEY_PASSWORD

# React Native / Expo defaults
hermesEnabled=true
android.useAndroidX=true
android.enableJetifier=true
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8
