# Supabase Configuration
# Copy this file to .env and fill in your Supabase credentials
# Get these values from Supabase Dashboard > Project Settings > API

# Required Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (Recommended for seeding scripts - bypasses RLS)
# Get this from: Supabase Dashboard > Project Settings > API > service_role key
# WARNING: Keep this secret! Never commit it to git or expose it in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: EAS projectId is in app.json (expo.extra.eas.projectId)
# For production: run `npx eas init` to link to your EAS account

# Instructions:
# 1. Go to https://supabase.com/
# 2. Sign up or log in
# 3. Create a new project (or select existing one)
# 4. Go to Project Settings > API
# 5. Copy the "Project URL" to EXPO_PUBLIC_SUPABASE_URL
# 6. Copy the "anon public" key to EXPO_PUBLIC_SUPABASE_ANON_KEY
# 7. Copy the "service_role" key to SUPABASE_SERVICE_ROLE_KEY (for seeding)
# 8. Save this file as .env (without .example)
# 9. Run the database migration: supabase/migrations/001_initial_schema.sql
# 10. Run: node scripts/seedArticlesWeb.js
# 11. Restart your Expo development server
