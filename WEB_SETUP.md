# راهنمای اجرا در مرورگر وب

## ⚠️ نکات مهم

برخی از ویژگی‌ها در مرورگر وب کار نمی‌کنند:
- ❌ **Location/GPS**: نیاز به دستگاه واقعی دارد
- ❌ **Compass/Magnetometer**: نیاز به حسگرهای دستگاه دارد
- ❌ **Notifications**: در مرورگر محدود است
- ❌ **Haptic Feedback**: فقط در موبایل

✅ **ویژگی‌های کار می‌کنند:**
- ✅ قرآن و تلاوت
- ✅ اذکار و شمارنده
- ✅ تقویم اسلامی
- ✅ برنامه رمضان
- ✅ تنظیمات و آمار

## 🚀 روش اجرا

### روش 1: دستور مستقیم
```bash
npx expo start --web
```

### روش 2: از Expo CLI
```bash
npx expo start
# سپس دکمه 'w' را بزنید
```

## 🌐 آدرس‌ها

بعد از اجرا، این آدرس‌ها در دسترس هستند:
- **Metro Bundler**: http://localhost:8081
- **Web App**: http://localhost:8081 (یا پورت دیگری که نمایش داده می‌شود)

## 🔧 اگر صفحه خالی بود

1. **صبر کنید**: ممکن است 30-60 ثانیه طول بکشد تا bundler آماده شود
2. **Refresh کنید**: F5 یا Cmd/Ctrl + R
3. **Console را بررسی کنید**: F12 → Console برای خطاها
4. **Cache را پاک کنید**:
   ```bash
   npx expo start --web --clear
   ```

## 📱 برای تست کامل

برای تست همه ویژگی‌ها (Location, Compass, etc.) باید از:
- **Expo Go** روی گوشی واقعی استفاده کنید
- یا **Android/iOS Simulator**
